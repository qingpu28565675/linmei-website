# 林美園藝官網 網站架構（2026-07-28 定案）

定位是**資格審查型**：讀者是標案承辦與廠辦總務，他們要在三分鐘內確認「這家公司做過什麼、有沒有資格、找誰談」。
所以導覽只有四個主項目＋一個詢價鈕，任何一頁都能兩步走到實績與聯絡。

## 網址結構

| 網址 | 頁面 | 目的 | 狀態 |
| :-- | :-- | :-- | :-- |
| `/` | 首頁 | 一頁看完憑證：數字→實績→能力→證照→聯絡 | ✅ |
| `/services/` | 服務項目總覽 | 四類服務入口＋施工流程 | ✅ |
| `/services/landscape/` | 景觀綠化施作 | SEO：景觀綠化工程 × 地區 | ✅ |
| `/services/pruning/` | 樹木修剪與移植 | SEO：樹木修剪 × 地區（主力字） | ✅ |
| `/services/lawn/` | 草皮養護 | SEO：草皮維護 × 地區 | ✅ |
| `/services/pest/` | 病蟲害防治 | SEO：褐根病／樹木健檢 | ✅ |
| `/works/` | 工程實績列表 | 分類篩選＋分頁，資格審查主戰場 | ✅ |
| `/works/case-001/` … `case-006/` | 實績單頁 | 每案：概要／施工重點／施工紀錄／工程資料表 | ✅ 6 篇 |
| `/about/` | 關於我們 | 公司沿革、工程數字、證照、團隊與設備 | ✅ |
| `/news/` | 最新消息列表 | 證明公司在動、證照有在更新 | ✅ |
| `/news/YYYY-MM-DD-slug/` | 消息單頁 | | ✅ 1 篇 |
| `/contact/` | 聯絡與詢價 | 電話為主、表單為輔、LINE 收在私人客戶區 | ✅ |

**網址規則**：一律用資料夾＋`index.html`，網址結尾帶斜線（`/works/case-001/`），不露出 `.html`。
實績與消息的 slug 用「編號」與「日期-主題」，之後接 Firebase 後台時由後台產生。

## 第二階段（等素材／後台）

- `/works/` 分頁目前是靜態三頁按鈕，接後台後改由資料筆數產生
- 地區頁 `/areas/taoyuan/` 等：等服務頁字詞有排名基礎再做，避免一次鋪太多薄內容頁
- 服務子頁底部的「服務 × 地區」交叉連結目前指向 `#`，地區頁上線後接上

## 內部連結設計（刻意的，不要隨手改）

- 首頁「施工能力」四格 → 直接連四個服務子頁（首頁流量餵給 SEO 著陸頁）
- 每個實績單頁 → 連回它所屬的服務子頁＋另外兩個實績（互相導流，降低跳出）
- 每個服務子頁 → 連 2-3 個同類實績（「說得到也做得到」的證明就在下一頁）
- 全站頁尾固定露出四個服務子頁，確保爬蟲每頁都能抓到

## 共用區塊的維護方式

這是純靜態站，沒有 include 機制，所以 header／drawer／cta-band／footer 是**每頁各一份複製**。
要改導覽或頁尾時，不要手動一頁一頁改——請 Claude 批次替換所有 `index.html`，改完用下面的方式驗一次。

樣式與腳本只有兩支共用檔，不要在頁面裡寫 `<style>`：
- `/assets/site.css`
- `/assets/site.js`

## 本機預覽

網站用絕對路徑（`/assets/site.css`），直接用瀏覽器開檔案會壞，要起一個本機伺服器：

```bash
python3 -m http.server 8788 --bind 127.0.0.1 --directory /Users/Shared/Claude-Code/linmei-website
```

然後開 http://localhost:8788/
（工作區 `.claude/launch.json` 已有 `linmei` 這組設定，跟 Claude 說「開預覽」就會起這個。）
