# 林美園藝官網 — 首頁設計方向（2026-07-28）

定位：**資格審查型**。主要讀者＝標案承辦人員、廠辦總務，他們來網站是要查證「這家公司可不可靠」。
次要轉換＝私人客戶加 LINE，動線刻意收在最後，不與標案讀者搶。

---

## 一、參考站拆解（動工前實際載入量測，非印象）

| 站 | 位置 | 抄了什麼 |
| :-- | :-- | :-- |
| 石勝エクステリア（東急不動產集團） | ishikatsuext.jp | 用**可稽核的工程數字＋附註量測方法**取代「20 年經驗」口號；畫布不用純白；兩支進場動畫重複用到底 |
| 西武造園 | seibu-la.co.jp | Hero **只做半高**，讓內容立刻出現；實績放在很前面、是全頁最大區塊；「取得認定」當正式欄位 |
| 榮工工程 | rseaec.com.tw | 同語系的資格審查樣本：實績卡欄位＝**案名／業主／施工地點／年份**；品牌色只用在文字與 1px 線 |
| 東急建設 | tokyu-cnst.co.jp | **紅色品牌卻整站 CSS 零紅色**，紅只活在 logo 檔裡 |

**關鍵發現：四站沒有一個做「施工前後對比」。** 前後對比是零售（私人庭園）語彙，不是資格審查語彙。所以案例卡不做前後對比，改放業主與地點。

---

## 二、配色：60 / 30 / 8 / 2

| 比例 | 角色 | 值 |
| :-- | :-- | :-- |
| 60% | 工程實景照片（真正的主色） | — |
| 30% | 中性畫布 | `#F4F3F0`、`#EAE8E3`；文字 `#1F1D1A`（暖近黑，不用純黑） |
| 8% | 結構深色 | 森林綠 `#1E3A2B`（施工能力區、頁尾整塊） |
| 2% | **紅色** | `#C8102E` |

紅色只做五件事：logo 標記、英文眉標旁的短線、證照勾選框、施工能力的編號、LINE 主 CTA。
**依據**：東急建設是紅色品牌卻整站不鋪紅；榮工的品牌藍出現 287 次但幾乎全是文字與 1px 線。
制服的紅是識別，不是版面主色——整片鋪紅會讀成促銷網站，不是工程公司。

## 三、排版（抄日系工程站的量測值）

中文大標 42px / 行高 1.5 / 字距 0.04em / w500；導言 20px / 行高 2.2；內文 16px / 行高 2.0。
英文眉標 13px 全大寫置於中文大標上方，標題字級比 **3:1**。字體只用兩支：`Inter + Noto Sans TC`（免費、不必買字型）。
標題與內文的差距靠**字級與留白**，不靠 bold。

## 四、首頁區塊順序

Hero（半高＋角落嵌最新消息）→ 一句話定位 → **三個工程數字（每個都附註算法與期間）** → **工程實績（最大區塊）** → 施工能力 4-up → 資格與證照＋公司概要表 → 最新消息 → 聯絡／LINE → 深綠頁尾

「先給憑證再給案例」的順序抄自石勝。

## 五、動畫：全站只有兩支

- A：`opacity 0→1`，0.4s linear（文字、清單）
- B：`scale 1.12→1 ＋ 淡入`，1s `cubic-bezier(.16,1,.3,1)`（＝GSAP 的 `expo.out`，工程照片用）

進場一律只播一次、`top 90%` 觸發。**不做 pin、不做視差 scrub**——四個工程公司參考站全都沒有。
Hero 三張底圖 5 秒交叉淡入＋緩慢推近（西武的節奏，不搶戲）。

技術註記：進場改用 scroll 事件＋幾何判斷驅動 CSS transition，不依賴 rAF／IntersectionObserver，
確保任何渲染環境都不會把內容卡在隱藏狀態；GSAP 保留給 Hero 圖層輪播。

---

## 五之二、內頁（2026-07-28 補）

網站架構、網址規則、內部連結設計都寫在 **`SITEMAP.md`**，改架構前先看那份。

內頁沿用首頁的同一套規則，只多了三個元件：
- `.page-head`：內頁標頭，深綠底＋工程照（`opacity:.42`）＋左重右輕的漸層，取代首頁的半高 HERO。高度約首頁 HERO 的四分之一——資格審查的讀者要的是內容，不是氣氛。
- `.crumbs`：麵包屑，緊接標頭下方。深色標頭 → 細麵包屑 → 內容，這個節奏抄自榮工。
- `.cta-band`：每個內頁結尾的深綠 CTA 帶，左文案右電話。**電話用 Inter 大字，是全頁最大的數字**——標案承辦最後要的就是一支能打的電話。首頁不放（首頁本來就有完整聯絡區）。

樣式與腳本只有兩支共用檔：`assets/site.css`、`assets/site.js`。**頁面裡不要寫 `<style>`**，微調用 inline style。
GSAP 只有首頁需要（HERO 輪播），內頁不引。

## 六、目前是 demo，還缺什麼

- [ ] **所有照片都是暫用示意圖**（Unsplash 免費圖，已逐張確認內容對題：廠辦綠帶、鏈鋸修樹、割草機草坪養護等），等朋友回傳工作照與案場照後全部替換。替換時記得沿用 `filter:saturate(.92) contrast(1.14) brightness(.94)`，讓不同來源的照片看起來像同一組
- [ ] 公司概要的負責人、統編、電話、地址、Email 全是佔位符
- [ ] 三個工程數字（168 案／42,600 ㎡／9,340 株）是**示意數字**，要換成實際可稽核的數字，附註也要跟著改
- [ ] 案例的業主名稱以 ○○ 代替，實際刊登前要確認哪些業主可具名（標案通常可，私人客戶要問過）
- [ ] logo 目前是「紅色直線＋黑體字標」的簡易處理，之後可再做正式字標
- [ ] 實績分類篩選目前只切換按鈕狀態，實作要接 Firebase 後台資料
